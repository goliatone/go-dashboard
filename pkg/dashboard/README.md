# Public Dashboard Package

Small façade that re-exports the internal components so downstream
projects can import `github.com/goliatone/go-dashboard/pkg/dashboard`
without depending on internal paths.
